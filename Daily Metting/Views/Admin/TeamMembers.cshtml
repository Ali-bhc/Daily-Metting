@using Daily_Metting.Models;
@model List<User>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<h1 class="text-center">Supply chain Team :</h1>
<table class="table">
    <thead class="thead-dark">
        <tr>
            <th class="text-center">
                Name :
            </th>
            <th class="text-center">
                Username :
            </th>
            <th class="text-center">
                Departement :
            </th>
            <th class="text-center">
                Is Active :
            </th>
            <th class="text-center">
                Missed Submissions :
            </th>
            <th class="text-center">
                Update :
            </th>

            
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.Departement)
                </td>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.IsActive)
                </td>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.MissedSubmissions)
                </td>
                <td class="text-center">
                    <a asp-controller="Admin" asp-action="Update_User" asp-route-id="@item.Id">Edit</a>               
                </td>

                @*<form asp-controller="Admin" asp-action="Update_User">
                    <input type = "hidden" value=@item.UserName />
                    <button>Edit</button>
                </form>*@
@*            </td>
*@            </tr>
        }

    </tbody>

</table>